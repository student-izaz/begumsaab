const Product = require('../models/Product')

const filterByPrice = async (res, req) => {
    const price = req.params;
    console.log(params);
    try {
        // Product.find({price: })
    } catch (error) {
        
    }
}